// import { PaginationData } from "@/models";

// export function isPaginated<T>(obj: T): obj is PaginationData<T> {
//   return typeof obj === "object" && obj !== null && "current_page" in obj;
// }
